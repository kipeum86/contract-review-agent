{
  "permissions": {
    "allow": [
      "Bash(find \"/c/Users/localuser/Desktop/VS Code/contract-review-agent/.claude/skills\" -name \"*.py\" -type f -exec wc -l {} +)",
      "Bash(kill %1)",
      "Edit(/.claude/skills/docx-redliner/**)",
      "Edit(/.claude/skills/doc-parser/**)",
      "Edit(/.claude/skills/index-manager/**)",
      "Edit(/.claude/skills/clause-segmenter/**)",
      "Edit(/.claude/skills/report-compiler/**)",
      "Edit(/.claude/skills/review-domain-knowledge/**)",
      "Bash(rm generate_cdkey_template.py)",
      "Bash(rm -f CD_Key_Distribution_Agreement_Template_Outline.docx)",
      "Bash(ls -la \"/path/to/contract-review-agent/output/\"*.docx)",
      "Bash(find \"/path/to/GDPR-expert\" -type f -name \"*.py\" -exec grep -l \"def.*search\\\\|def.*query\\\\|def.*retrieve\" {} \\\\;)"
    ]
  },
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/inject-domain-references.sh",
            "timeout": 5
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"\nimport json, os, re, sys\n\ntool_input = os.environ.get('TOOL_INPUT', '{}')\ntry:\n    data = json.loads(tool_input)\nexcept Exception:\n    sys.exit(0)\n\nproject_root = os.environ.get('PROJECT_DIR', os.getcwd())\nfile_path = data.get('file_path', '')\ncommand = data.get('command') or data.get('cmd') or ''\n\nif file_path:\n    allowed_prefixes = [\n        os.path.join(project_root, 'contract-review'),\n        os.path.join(project_root, '.claude'),\n        os.path.join(project_root, 'docs'),\n        os.path.join(project_root, 'CLAUDE.md'),\n        os.path.join(project_root, 'package.json'),\n        os.path.join(project_root, 'package-lock.json'),\n        os.path.join(project_root, 'node_modules')\n    ]\n    abs_path = os.path.abspath(file_path)\n    if not any(abs_path.startswith(p) for p in allowed_prefixes):\n        print(f'BLOCKED: Write to {abs_path} is outside allowed directories', file=sys.stderr)\n        sys.exit(1)\n\nif command:\n    normalized_command = command.lower()\n    approved_markers = [\n        'contract-review/library/approved',\n        'approved/templates',\n        'approved/precedents',\n        'approved/playbooks',\n        'approved/comment-bank'\n    ]\n    allowed_publishers = [\n        'scripts/batch_classify_and_publish.py',\n        '.claude/skills/index-manager/scripts/supersession.py',\n        '.claude/skills/index-manager/scripts/build-index.py'\n    ]\n    direct_write_re = re.compile(r'\\b(cp|mv|rsync|install|ditto|mkdir|tee)\\b|(?:^|\\s)(>|>>)(?=\\s)')\n    if any(marker in normalized_command for marker in approved_markers) and direct_write_re.search(command) and not any(p in normalized_command for p in allowed_publishers):\n        print('BLOCKED: Direct Bash writes to contract-review/library/approved are disallowed. Use the publish workflow or index-manager service.', file=sys.stderr)\n        sys.exit(1)\n\""
          }
        ]
      }
    ]
  }
}
